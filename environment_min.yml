name: dl
channels:
  - conda-forge
  - defaults

dependencies:
  - python=3.10
  - pip

  # Core scientific / vision stack
  - numpy=1.26
  - scipy
  - scikit-learn
  - opencv
  - pillow
  - matplotlib
  - pandas
  - tqdm
  - pyyaml
  - imageio
  - scikit-image

  # Backend server
  - fastapi
  - uvicorn
  - pydantic
  - python-multipart
  - requests

  # Streaming / media
  - av

  - pip:
      # WebRTC / streaming
      - aiortc

      # Deep learning / feature extraction utilities
      - timm
      - einops
      - fast-pytorch-kmeans

      # Optional project utilities
      - psutil
      - termcolor
    
      - httpx